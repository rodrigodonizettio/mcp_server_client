[project]
name = "weather-client"
version = "0.1.0"
description = "Weather MCP Client using AWS Bedrock models"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.71.0",
    "mcp>=1.19.0",
    "python-dotenv>=1.2.1",
    "boto3>=1.34.0",
]
